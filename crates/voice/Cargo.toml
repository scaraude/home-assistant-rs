[package]
name = "voice"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Voice I/O: Vosk (offline STT) + Piper (offline TTS) + ElevenLabs (API)"

[dependencies]
tokio.workspace = true
serde.workspace = true
anyhow.workspace = true

[features]
default = ["offline"]
offline = []          # Vosk + Piper (always works, no Internet)
elevenlabs = []       # ElevenLabs v3 TTS + STT (API, optional)
